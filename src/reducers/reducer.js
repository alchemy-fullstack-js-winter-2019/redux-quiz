const initialState = {
    title: 'faux Title',
    subTitle: 'faux subTitle'
};

export default function reducer(state =  initialState, action) {
    switch(action.type) {
        default:
            return state; 
    }
};

